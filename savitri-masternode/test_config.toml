# Test masternode configuration
role = "Masternode"

# Network identity key path
network_key_path = "test_identity.key"

# Masternode signing key path  
masternode_key_path = "test_masternode.key"

# P2P configuration
p2p_port = 4021

# Storage path
storage_path = "test_storage"

# Validators list (include local node)
validators = [
    "12D3KooWTestValidator1234567890ABCDEF",
]

# Bootstrap peers
bootstrap_peers = []

# Monolith configuration
monolith_interval_secs = 30
monolith_max_blocks = 10000

# Block production
tx_interval_secs = 1
block_interval_secs = 5
max_block_txs = 64
